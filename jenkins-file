pipeline {
    agent any

    stages {
        stage('git pull') {
            steps {
             build quietPeriod: 5, job: 'Git pull'
            }
        }
        stage('git status') {
            steps {
             build quietPeriod: 5, job: 'Git status'
            }
        }
        
    }
}
